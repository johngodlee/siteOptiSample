# Assess plot representativeness within a site and optimally locate candidate plots

`repres.R` - Example workflow 

`func.R` - Functions called in `repres.R`

`dat/` - Example data, not synced with repo


